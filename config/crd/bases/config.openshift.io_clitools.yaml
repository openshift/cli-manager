
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: clitools.config.openshift.io
spec:
  group: config.openshift.io
  names:
    kind: CLITool
    listKind: CLIToolList
    plural: clitools
    singular: clitool
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: CLITool is the Schema for the clitools API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CLIToolSpec defines the desired state of CLITool
            properties:
              binaries:
                description: Binaries for the CLI tool
                items:
                  description: CLIToolBinary defines per-OS and per-Arch binaries
                    for the given tool.
                  properties:
                    arch:
                      description: Architecture is the CPU architecture for given
                        binary (i.e. amd64, arm64)
                      type: string
                    image:
                      description: Image containing CLI tool
                      type: string
                    imagePullSecret:
                      description: ImagePullSecret to use when connecting to an image
                        registry that requires authentication
                      type: string
                    os:
                      description: OS is the operating system for the given binary
                        (i.e. linux, darwin, windows)
                      type: string
                    path:
                      description: Path is the location within the image where the
                        CLI tool can be found
                      type: string
                  type: object
                type: array
              description:
                description: Description of the CLI tool
                type: string
            type: object
          status:
            description: CLIToolStatus defines the observed state of CLITool
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
